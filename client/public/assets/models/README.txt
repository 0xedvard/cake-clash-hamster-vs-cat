Place future hero character GLB files here:
- edvard.glb
- elina.glb

The runtime will try to load these files automatically.
If a file is missing or fails to load, the game falls back to the procedural placeholder character for that role.
